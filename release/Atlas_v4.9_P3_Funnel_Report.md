# Atlas v4.9 P3 用户漏斗报告（Funnel Report）

> 性质：真实用户验证 · 数据标记 REAL_USER
> ⚠️ 当前真实用户 = 1（产品负责人），以下漏斗基于真实埋点 + 模拟参照，**明确分开**

## 一、漏斗定义

```
安装(install) → 首次打开(first_open) → 引导完成(onboarding) → 保存彩票(ticket_saved)
→ 开奖提醒(draw_reminder_clicked) → 查看中奖(claim_checked) → 查看报告(report_viewed)
```

## 二、真实漏斗（REAL，当前 1 用户）

基于 `~/.atlas/analytics_v46.jsonl`（128 事件，1 真实用户）：

| 阶段 | 用户 | 转化率 | 说明 |
|------|-----:|-------:|------|
| 安装 | 0 | — | 旧埋点不记录 install（桌面启动即打开）|
| 首次打开 | 1 | 100% | app_opened 115 次 |
| 引导完成 | 1 | 100% | onboarding_complete 13 次 |
| 保存彩票 | 1 | 100% | 40 张票据在库 |
| 开奖提醒 | 0 | 0% | 提醒点击未埋点到 v46 |
| 查看中奖 | 0 | 0% | 兑奖在 events_v43（字段不兼容）|

> **1 个用户无法形成有意义的漏斗**——仅证明"埋点链路工作"，非用户价值证据。

## 三、模拟参照漏斗（SIMULATION，seed=42，100 用户）

> 明确标注：**合成数据，仅作管道验证参照，不是真实用户表现**

```
安装 100 → 首次打开 83（83%）→ 保存 52（52%）→ 提醒 18（18%）
→ 兑奖 9（9%）→ 周报 6（6%）
```

**管道验证作用**：证明漏斗计算正确（每阶段人数/转化/流失），且能暴露理论流失点（保存→提醒 65% 流失）。

## 四、与 P1 的差异说明（诚实）

- P1 报告的 Q1 83% / Q2 52% / D1 47% / D7 47% / WALU 52 全部为**模拟数据**
- P3 已将这些标记为 **SIMULATION**，与真实数据严格分离
- **真实漏斗需要 50 人实际使用后才有意义**

## 五、结论

**漏斗基础设施就绪，真实漏斗数据空缺**。目标（首次建档率 ≥50%）需 50 人真实采集后判定。
