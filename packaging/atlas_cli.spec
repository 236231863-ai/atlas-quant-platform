# -*- mode: python ; coding: utf-8 -*-
# Atlas Quant Platform - CLI Executable Spec
# 打包 Atlas_CLI.exe（命令行平台管理工具）

a = Analysis(
    ['..\\tools\\atlas-cli\\main.py'],
    pathex=['..\\tools\\atlas-cli'],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Atlas_CLI',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
